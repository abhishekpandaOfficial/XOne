# XOne native icons

These application icons are generated from the approved `X1.png` artwork supplied
for XOne. `tauri.conf.json` references this directory directly for macOS, Windows,
and Linux bundles. The preserved upstream icons remain outside this directory for
attribution and compatibility history, but are no longer used by XOne builds.
